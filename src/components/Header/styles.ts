import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  headerText: {
    textAlign: 'center',
    fontSize: 30,
    color: '#fff',
    fontWeight: 'bold',
    paddingTop: 20,
  },
});
